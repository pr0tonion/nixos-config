# STUB — replace before deploying to real hardware.
# On the target machine run:
#   sudo nixos-generate-config --show-hardware-config > hosts/home-computer/hardware-configuration.nix
# Then commit the result.
{ ... }: {}
